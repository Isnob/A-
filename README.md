# A-
Python tool for visualizing grid-based pathfinding algorithms (A* and others). Shows visited, active, and final path cells with smooth animation. Supports importing maze files and using a config.json for sizes, colors, and speed. Built with OOP and Pygame. Expandable with more algorithms and UI features.
